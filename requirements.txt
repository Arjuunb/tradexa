# Vercel Python runtime marker — handlers use stdlib only.
# This file must contain at least one dependency for Vercel to detect
# the Python runtime and pick up api/*.py as serverless functions.
requests==2.31.0
